import styled, { css } from 'styled-components'
import Head from 'next/head'

const gear = () => {
    return (
        <div>
            <h1 style={{ height: '80rem' }}>Gear Page</h1>
        </div>
    )
}

export default gear
