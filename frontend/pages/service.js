import styled, { css } from 'styled-components'
import Head from 'next/head'

const service = () => {
    return (
        <div>
            <h1 style={{ height: '80rem' }}>Service Page</h1>
        </div>
    )
}

export default service
